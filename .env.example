# Example .env file
# Copy this file to .env and modify the values below

# Required
KEY_PAIR=aws-ec2-tee-keypair

# Optional (defaults will be used if not set)
REGION=us-east-1
AMI_ID=ami-085ad6ae776d8f09c
API_ENV_VAR_NAME=API_KEY
EC2_INSTANCE_NAME=my-enclave
INSTANCE_TYPE=m5.xlarge  # Instance type (ARM64 architecture)
# INSTANCE_ID=i-0123456789abcdef0  # If set, use this existing instance instead of creating a new one
ENCLAVE_APP=twitter-example

# VPC configuration (optional)
# VPC_ID=  # If set, use the specified VPC; otherwise select an existing VPC or create a new one

# Other optional settings
# USE_SECRET=y  # Whether to use a secret (y/n)
# SECRET_CHOICE=new  # Create a new secret or use an existing one (new/existing)
USER_SECRET_NAME=API_KEY  # New secret name
SECRET_VALUE=SECRET_VALUE  # Secret value